metadata:
  name: "Ensure Cloud SQL instances do not configure authorized networks"
  id: "CKV_GCP_CUSTOM_2"
  category: "NETWORKING"
  severity: "HIGH"
  description: "Ensure authorized networks are not configured on Cloud SQL instances."

definition:
  resource_types:
    - "google_sql_database_instance"
  cond_type: "attribute"
  attribute: "settings.ip_configuration.authorized_networks"
  operator: "not_exists"
